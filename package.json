{
  "name": "yfi-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:scss": "sass src/styles/index.scss www/css/styles.css",
    "dist": "npm run dev:scss && cd www && zip -r ../deployment.zip . -x \"*.map\" -x \".DS_Store\" -x \"*/.DS_Store\" && cd .."
  },
  "author": "Michael Rose",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.66.1"
  }
}
